# CyberShield Calibration Status

This report is generated by `tools/validate_model_calibration.py`.

## Summary

- Catalog models checked: 20
- Blocking findings: 0
- Calibration warnings: 1
- Status: PASS

## Decision Roles

| Model | Role | Runtime use |
| --- | --- | --- |
| `android_malware` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `mirai` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `network_attack` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `dns_stateful` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `doh_l1` | `routing_only` | Routes signal to another model; no direct alert. |
| `doh_l2` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `social_text` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `social_url` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `phishing_html` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `stealth_phisher_2025` | `primary_detection` | Can raise user-visible intervention when corroborated by policy. |
| `iot_attack` | `field_calibration_required` | Can warn or increase protection after field/lab calibration. |
| `attack_anomaly` | `support_only` | Risk support signal only; no standalone hard blocking. |
| `post_quantum` | `field_calibration_required` | Can warn or increase protection after field/lab calibration. |
| `post_quantum_taxonomy` | `support_only` | Risk support signal only; no standalone hard blocking. |
| `post_quantum_subtype` | `support_only` | Risk support signal only; no standalone hard blocking. |
| `mitm_arp` | `field_calibration_required` | Can warn or increase protection after field/lab calibration. |
| `wifi_threat` | `field_calibration_required` | Can warn or increase protection after field/lab calibration. |
| `android_malware_flow` | `support_only` | Risk support signal only; no standalone hard blocking. |
| `honeypot_threat_intel` | `support_only` | Risk support signal only; no standalone hard blocking. |
| `contextual_fraud_anomaly` | `support_only` | Risk support signal only; no standalone hard blocking. |

## Findings

- `warn` `wifi_threat`: near-perfect dataset score; require field calibration evidence

## Required External Evidence

- AMTSO Android malware and phishing feature checks.
- Benign URL/SMS/APK false-positive corpus.
- DNS leak and VPN forwarding verification.
- Wi-Fi MITM/ARP lab replay or controlled field capture.
- 24-48 hour battery, RAM, CPU and network-load run.
- OWASP MASVS/MASA-style independent review before production certification claims.

This report is a calibration gate, not an independent certification.
